# snakemake_variants-call
A Snakemake workflow (pipeline) for variants calling from the Illumina paired-end data
# Config file:
Prior to execute the workflow, the config file must be spcified to configure paths to reference genome, and additional data files required by the GATK.
